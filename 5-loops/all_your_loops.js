// 1 - Write a for loop that runs 5 times
// Each iteration will increase the value
// of checkz by 1
var checkz = 0;
for (var i = 0; i <= 4, i++){
  checkz += 1;
}
// 2 - Write a while loop that runs
// 3 times. Each iteration will decrease
// the value of checkz by -2
while (i < 3) {
    text += "The number is " + i;
    i++;
}
// 3 - Summarize in English what this code is doing.
// Use a multiline comment to do so.
//
var numbers = [1, 2, 3, 4, 5, 6, 7];
var total = 0;
for (var i = 0; i < numbers.length; i++) {
  total += numbers[i];
}
console.log(total);

// This silly little function goes over the
// length of the numbers array and adds the
// totals of each array position together.
//It adds 1+2+3+4+5+6+7 to the total variable
//then of course console logs var total
